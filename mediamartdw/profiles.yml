mediamartdw:

  target: dev

  outputs:
    dev:
      type: sqlserver

      driver: ODBC Driver 18 for SQL Server

      host: localhost
      port: 1433
      database: MediaMartDW
      schema: dbo

      windows_login: True

      encrypt: false

